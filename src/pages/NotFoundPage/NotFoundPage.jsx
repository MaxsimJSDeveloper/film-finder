export default function NotFoundPage() {
  return (
    <main
      style={{
        display: "flex",
        flexDirection: "column",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <p>Page not found</p>
      <a href="/">Go to Home</a>
    </main>
  );
}
